import calendar
# Create a plain text calendar
c = calendar.TextCalendar(calendar.SUNDAY)
str = c.formatmonth(2020, 3, 0, 0)
print(str)

class workoutSession:
    def __init__(self, date, personalRoutine)
        self.date = calendar.date
        self.personalRoutine = personalRoutine

    

